/**
 * Placeholder: mesas y cuentas (sprints 3 a 6).
 * Acceso: solo plan salon/completo; dueño y mozo (ver el menú en
 * src/app/app/layout.tsx).
 */
export default function PaginaMesas() {
  return <h1 className="text-xl font-semibold text-neutral-900">Mesas</h1>;
}
