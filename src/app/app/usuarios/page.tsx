/**
 * Placeholder: alta/baja del personal del comercio (fuera del Plan 1
 * descripto en docs/SCHEMA.md, pero el link ya se necesita en el menú).
 * Acceso: solo dueño (ver el menú en src/app/app/layout.tsx).
 */
export default function PaginaUsuarios() {
  return <h1 className="text-xl font-semibold text-neutral-900">Usuarios</h1>;
}
